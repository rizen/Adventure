package Adventure::Module::ActionCastle::Action::TakeItem;

sub main {
    # my ($class, $thing_that_has_object, $params) = @_;
    # if (Adventure->item_exists($params->{item})) {
    #     if (Adventure->actor->has_item($params->{item}, $params->{quantity})) {
    #         Adventure->actor->remove_item($params->{item}, $params->{quantity});
    #         Adventure->player->add_item($params->{item}, $params->{quantity});
    #         Adventure->player->announce('You have a '.Adventure->game->item($params->{item}).'.');
    #     }
    #     else {
    #         Adventure->player->announce(Adventure->actor->name.' has no '.Adventure->game->item($params->{item}).'.');
    #     }
    # }
    # else {
    #     Adventure->player->announce("I don't know what that is.");
    # }
}

1;
